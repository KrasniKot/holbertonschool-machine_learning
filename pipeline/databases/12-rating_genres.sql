-- Retrieve rating by genre
SELECT tg.name, SUM(tsr.rate) AS rating FROM tv_genres tg, tv_show_genres tsg, tv_show_ratings tsr WHERE tsg.genre_id = tg.id AND tsr.show_id = tsg.show_id GROUP BY tg.name ORDER BY rating DESC;